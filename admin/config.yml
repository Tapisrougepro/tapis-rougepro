backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
    - name: "pages"
    label: "Pages du site"
    files:
            - label: "Page d'accueil"
        name: "home"
        file: "index.html"
        fields:
                    - {label: "Contenu", name: "body", widget: "code"}
